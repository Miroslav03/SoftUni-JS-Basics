function Sequence(input){

    let end=Number(input[0])
    let starting=1
    //console.log(starting);

    while (starting<=end) {
        console.log(starting);
        starting= (2*starting)+1
        
    }


} 
Sequence(["17"]) 